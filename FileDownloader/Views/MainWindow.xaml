<mah:MetroWindow x:Class="FileDownloader.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:viewModels="clr-namespace:FileDownloader.ViewModels"
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance viewModels:MainViewModel, IsDesignTimeCreatable=True}"
                 Title="FileDownloader" Height="450" Width="800">
    <Grid>
        <!-- ... другие элементы ... -->

        <Button Content="Test Insert Attachment"
                Command="{Binding TestInsertCommand}"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="10"/>

        <!-- ... другие элементы ... -->
    </Grid>
</mah:MetroWindow> 